# TSXBot requirements for Render deployment
# Render will use this to install dependencies

# Core dependencies (from pyproject.toml)
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.25
aiohttp>=3.9

# Supabase (optional, for cloud persistence)
supabase>=2.0

# OpenAI (for AI features)
openai>=1.0
